version: 0.1


phases:
  install:
    commands:
      npm install
    finally:
      - echo Install complete  
  build:
    commands:
      npm run  build
    finally:
      - echo build complete
artifacts:
  files: imagedefinitions.json