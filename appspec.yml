
version: 0.0
os: linux
files:
  - source: / 
    destination: /home/centos/cdtest
permissions:
  - object: /
    pattern: "**"
    owner: centos
    group: centos
hooks:
  ApplicationStart:
    - location: scripts/start_server
      timeout: 300